{
  "name": "remembeer-firestore-rules",
  "private": true,
  "scripts": {
    "test": "node --test test/firestore-rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.2.1",
    "firebase-tools": "^14.15.2"
  }
}
